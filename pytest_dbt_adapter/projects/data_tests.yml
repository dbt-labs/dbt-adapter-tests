name: data_tests
paths:
    tests/passing.sql: files.tests.passing
    tests/failing.sql: files.tests.failing
dbt_project_yml:
    test-paths:
        - tests
    seed-paths:
        - data
facts:
    test:
        length: 2
        names:
            - passing
            - failing
